name: ordo_app
description: Ordo - Your Solana DeFi Assistant
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Typography
  google_fonts: ^6.1.0
  fl_chart: ^0.66.0
  cached_network_image: ^3.3.1
  
  # State Management
  provider: ^6.1.1
  
  # Backend & API
  http: ^1.2.0
  
  # Voice & Accessibility
  # speech_to_text: ^6.6.0  # Temporarily disabled - compatibility issue
  flutter_tts: ^3.8.5
  permission_handler: ^11.3.0
  
  # Platform Integration
  # uni_links: ^0.5.1  # Deprecated, replaced by app_links
  app_links: ^6.3.4
  
  # Security & Storage
  flutter_secure_storage: ^10.0.0
  local_auth: ^3.0.0
  shared_preferences: ^2.2.2
  
  # Utils
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # assets:
  #   - assets/chains/
